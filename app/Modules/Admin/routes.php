<?php

// Reserved for Admin-module routes beyond the staff dashboard and 2FA setup
// already registered in routes/admin.php (user management, vendor approval,
// vendor fee settings, reports, role/permission management). This file is
// required directly from routes/admin.php, inside the auth + 2FA-enrolled
// middleware group, so anything added here inherits both automatically.
